#include "lib.h"
#include <stdio.h>
#include <unistd.h>
#include <termios.h> 
#include <fcntl.h>   

void kirmizi() {
    printf("k\n");
    fflush(stdout);
}

void sari() {
    printf("s\n");
    fflush(stdout);
}

void yesil() {
    printf("y\n");
    fflush(stdout);
}

// Karmaşık işletim sistemi kodlarını buraya hapsettik
int tusa_basildi_mi() {
    struct termios oldt, newt;
    int ch;
    int oldf;
    
    tcgetattr(STDIN_FILENO, &oldt);
    newt = oldt;
    newt.c_lflag &= ~(ICANON | ECHO);
    tcsetattr(STDIN_FILENO, TCSANOW, &newt);
    oldf = fcntl(STDIN_FILENO, F_GETFL, 0);
    fcntl(STDIN_FILENO, F_SETFL, oldf | O_NONBLOCK);
    
    ch = getchar();
    
    tcsetattr(STDIN_FILENO, TCSANOW, &oldt);
    fcntl(STDIN_FILENO, F_SETFL, oldf);

    // Eğer bir tuşa basıldıysa ve bu tuş '1' ise onay ver
    if(ch != EOF && ch == '1') {
        return 1;
    }
    return 0; // Basılmadıysa veya başka tuşa basıldıysa 0 dön
}